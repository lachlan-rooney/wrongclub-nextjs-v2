The directory `/wrong-club/wrong-club/public` is intended for static assets like images and fonts that can be served directly. Since you haven't specified any particular files or assets to include in this directory, it will be left empty for now.

public directory is intentionally left blank.